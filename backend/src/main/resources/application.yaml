spring:
  application:
    name: demo
  datasource:
    url: jdbc:mysql://db:3306/memodb?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: rootpasswd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true